# Banco (preferencial: usar DATABASE_URL)
DATABASE_URL=

# Alternativa ao DATABASE_URL (use apenas se não usar URL completa)
TYPEORM_HOST=localhost
TYPEORM_PORT=5432
TYPEORM_USERNAME=postgres
TYPEORM_PASSWORD=
TYPEORM_DATABASE=postgres

# URL pública da aplicação (usada para gerar links/QR code)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
